---
to: pages/<%= h.changeCase.lcFirst(name) %>/sales.js
---
import React from 'react';
import Dashboard from 'src/screens/<%= h.changeCase.lcFirst(name) %>Sales';

import Page from 'src/components/Page';
import Head from 'next/head';

export default Page({
  loginRequired: true,
  logoutRequired: false,
  adminRequired: false,
  i18n: ['<%= h.changeCase.lcFirst(name) %>', 'common'],
  showSwitchLangBtn: true,
  displayName: 'DashboardPage',
})(() => (
  <React.Fragment>
    <Head>
      <title>Dashboard Sales</title>
    </Head>
    <Dashboard />
  </React.Fragment>
));
